# Artificial Intelligence Workbook Audit

대상: `data/subjects/artificial-intelligence/workbook.yaml`

원본: `origin/인공지능 워크북`

검토 기준:

- `docs/data-schema.md`의 워크북 ID, 지문/도표/이미지 처리 규칙
- 기존 운영체제 워크북 데이터셋의 `passages`, `diagram`, 선택지 이미지 구성 방식
- 원본 이미지를 10문항 단위로 대조

## 결과

- 총 94문항이 원본 1장~11장 워크북 문항과 대응한다.
- 원본 PNG는 43개이며, 여러 문항이 한 페이지 이미지에 포함되어 있다.
- 문제 본문, 선택지, 정답을 원본과 대조했다.
- 재현 가능한 트리, 표, 네트워크, 코드 지문은 `passages`와 `diagram`으로 구조화되어 있다.
- 원본의 패턴분포 그림을 봐야 하는 `b10-04`는 선택지별 crop 이미지로 연결했다.

## 1~10번

범위:

- `b01-01` ~ `b03-03`
- 원본 `1장/002.png` ~ `3장/002.png`

확인 내용:

- 물리적 기호 시스템, 지식공학, 연결주의, 상태공간 표현, 깊이우선 탐색, 균일비용 탐색 문항을 대조했다.
- `b02-02`의 쉼표 포함 선택지들이 YAML flow map에서 쉼표 앞까지만 파싱되는 문제를 확인하고 따옴표로 보정했다.

## 11~20번

범위:

- `b03-04` ~ `b05-01`
- 원본 `3장/002.png` ~ `5장/001.png`

확인 내용:

- 언덕오르기, 모의 담금질, A\*, 최대최소 탐색, 알파-베타 가지치기, 몬테카를로 트리 탐색, 지식기반 시스템 문항을 대조했다.
- 최대최소 탐색 트리는 `simple-graph` passage로 구조화되어 있다.

## 21~30번

범위:

- `b05-02` ~ `b06-01`
- 원본 `5장/001.png` ~ `6장/001.png`

확인 내용:

- 지식기반 시스템 구성요소, 메타지식, 선언적 지식, 시맨틱 네트, 규칙/프레임/전문가 시스템, 기호논리 문항을 대조했다.
- 시맨틱 네트는 `simple-graph` passage로 구조화되어 있다.
- 쉼표 포함 선택지 파싱 문제를 같은 방식으로 보정했다.

## 31~40번

범위:

- `b06-02` ~ `b07-01`
- 원본 `6장/002.png` ~ `7장/001.png`

확인 내용:

- 연언표준형, 추론, 긍정논법, 도출절, 도출연역, 술어논리, 퍼지집합 소속함수 문항을 대조했다.
- 절/술어 논리식의 수식 표기와 정답을 원본과 확인했다.

## 41~50번

범위:

- `b07-02` ~ `b08-02`
- 원본 `7장/001.png` ~ `8장/001.png`

확인 내용:

- 퍼지집합 소속함수, 합집합/여집합 표, 교집합 그래프, 퍼지논리 연산자, 퍼지추론, 디지털 영상 입력, 4-이웃 연결성 문항을 대조했다.
- 퍼지 표와 4-이웃 격자는 `data-table` diagram으로 구조화되어 있다.

## 51~60번

범위:

- `b08-03` ~ `b08-12`
- 원본 `8장/002.png` ~ `8장/004.png`

확인 내용:

- 평활화 필터, 중간값 필터, 소벨/라플라스 연산자, 4-이웃 경로, 히스토그램 임계값, 정규화, 특징추출, 거리측정자 문항을 대조했다.

## 61~70번

범위:

- `b08-13` ~ `b09-05`
- 원본 `8장/005.png` ~ `9장/003.png`

확인 내용:

- 도시블록 거리, 마할라노비스 거리, 최대가능도 추정, k-NN, 강화학습, 준지도학습, 귀납법, 재현율, 결정트리 문항을 대조했다.
- k-NN 선택지의 쉼표 포함 텍스트를 따옴표로 보정했다.

## 71~80번

범위:

- `b09-06` ~ `b10-05`
- 원본 `9장/003.png` ~ `10장/002.png`

확인 내용:

- 선형회귀, 교차 엔트로피, 다항 로지스틱 회귀, k-평균 알고리즘, 신경회로망, 퍼셉트론, 오차역전파 문항을 대조했다.
- `b10-04`는 기존 text passage가 정답을 직접 노출하고 있어 제거했다.
- `b10-04`의 네 패턴분포는 원본에서 선택지별 이미지로 crop하여 연결했다.

## 81~90번

범위:

- `b10-06` ~ `b11-06`
- 원본 `10장/003.png` ~ `11장/003.png`

확인 내용:

- RBM, 자기조직화 지도, LVQ, 딥러닝, 과적합, 경사 소멸, TensorFlow, CNN 문항을 대조했다.
- 쉼표 포함 선택지 파싱 문제를 같은 방식으로 보정했다.

## 91~94번

범위:

- `b11-07` ~ `b11-10`
- 원본 `11장/003.png` ~ `11장/005.png`

확인 내용:

- CNN 출력 크기, RNN, 어텐션, 트랜스포머 문항을 대조했다.
- CNN 출력 공식은 text passage로 분리되어 있다.

## 수정 사항

- 쉼표가 포함된 flow-map 선택지 텍스트를 따옴표로 감싸 실제 파싱 결과가 원문 전체 문구와 일치하도록 수정했다.
- `b10-04`의 정답 노출 text passage를 제거했다.
- `b10-04` 선택지 이미지 4개를 추가했다.

## 검증

- YAML 문항 수: 94
- 원본 PNG 수: 43
- 선택지의 예상 외 추가 키: 0
- 개별 YAML 스키마 검증: 통과
- 이미지 참조 누락: 0
- 전체 `pnpm data:build`: 통과
